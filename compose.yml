services:
  local-ip.sh:
    image: local-ip.sh
    build: .
    volumes:
      - lego:/local-ip/.lego
    restart: unless-stopped
    ports:
      - 53:53/udp
      - 80:80/tcp
      - 443:443/tcp

volumes:
  lego:
